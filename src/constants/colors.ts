/** Brand palette — keep in sync with CSS vars in `src/styles/index.css`. */
export const BRAND_COLORS = {
  black: '#000000',
  surface: '#2a2a2a',
  white: '#ffffff',
  green: '#c6f532',
} as const
