export const REPOS_PER_PAGE = 50;
/** How long a repos page stays fresh in memory. */
export const GIT_CACHE_TTL_MS = 5 * 60_000;
/** sessionStorage key for instant paint on refresh. */
export const GIT_SESSION_CACHE_KEY = '2h-hub:git-cache:v1';
